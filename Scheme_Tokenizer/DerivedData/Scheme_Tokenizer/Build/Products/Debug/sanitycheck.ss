symbol 1 1. 1.2 .2 car cdr cons if else and or not define number? symbol? lambda list? zero? null? char? string? error + - / * = > < >= <= ( ) '
; ignore this line please (cdr '(abcd) '(efgh))
"string literal test"
"unclosed string literal test (cdr '(abc) b)
and another"
symbol 1 1.2 define number? anobvious$error*thing (cdr '(car (cons '(abc) '(def)))
anobvious$error*thing2
(car '(c$dr) '(abc))
